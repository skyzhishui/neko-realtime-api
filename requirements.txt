# ============================================
# Neko Realtime API - Python Dependencies
# ============================================
# Core dependencies (required)
# ============================================

# Web framework & server
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# Async HTTP client (for Omni LLM, ASR, TTS HTTP calls)
aiohttp>=3.9.0

# WebSocket client (for TTS WS streaming & ASR WS)
websockets>=12.0

# Audio processing
numpy>=1.26.0

# Configuration
pyyaml>=6.0

# ============================================
# VAD & Smart Turn dependencies (required)
# ============================================

# ONNX Runtime - VAD & Smart Turn inference (ONNX-only, no PyTorch)
# Use onnxruntime-gpu for CUDA, onnxruntime for CPU, or onnxruntime-rocm for AMD ROCm
onnxruntime>=1.18.0

# Transformers - WhisperFeatureExtractor for Smart Turn
transformers>=4.40.0

# ============================================
# Model download & management
# ============================================

# ModelScope - auto-download VAD/SmartTurn/ASR models
modelscope>=1.18.0

# ============================================
# Local ASR dependencies (optional, enable if local_asr=true)
# ============================================

# sherpa-onnx - SenseVoice-Small ONNX inference (replaces FunASR)
sherpa-onnx>=1.10.0
